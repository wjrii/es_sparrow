# es_sparrow

> Generated by **QMK / Vial Wizard v5.6** on 2026-09-05  
> 🔗 https://www.robingosse.ca/qmkwizard  
> Made by **Robin Gosse**

---

## About This Firmware

| Property | Value |
|----------|-------|
| Keyboard | es_sparrow |
| Manufacturer | Handwired |
| Maintainer | wjrii |
| MCU | atmega32u4 |
| Bootloader | caterina |
| USB VID/PID | 0xFEED / 0x0001 |
| Form Factor | Monolithic (single board) |
| Matrix | 4 rows × 10 cols |
| Diode Direction | ROW2COL |
| Total Keys | 40 |
| Firmware System | Vial-QMK (https://www.robingosse.ca/qmkwizard) |

---

## Files in This Package

```
keyboards/handwired/es_sparrow/
├── keyboard.json              ← Main hardware config (pins, matrix, USB IDs, layout)
├── rules.mk                   ← Build system options (intentionally minimal)
└── keymaps/
    └── vial/
        ├── rules.mk           ← Enables Vial (VIAL_ENABLE = yes)
        ├── config.h           ← Vial UID, unlock combo, split handedness
        ├── keymap.c           ← Default keycodes (QWERTY starting point)
        └── vial.json          ← Vial GUI layout definition (compiled into firmware)

.github/
└── workflows/
    └── build.yml              ← GitHub Actions auto-build workflow
```

**What each file does:**

- **keyboard.json** — Tells QMK everything about your hardware: which pins are rows/columns,
  which way diodes are oriented, USB device name, and the physical position of each key.
  This is the only file that needs to change if you rewire anything.

- **rules.mk (keyboard level)** — Intentionally nearly empty. Hardware config lives in keyboard.json.

- **keymaps/vial/rules.mk** — Enables Vial. Must be at keymap level, NOT keyboard level.

- **keymaps/vial/config.h** — Sets the unique Vial keyboard ID and the security unlock key combo.

- **keymaps/vial/keymap.c** — Your starting keycodes. After flashing, use the Vial app to remap
  keys without reflashing. Think of this as just the factory default.

- **keymaps/vial/vial.json** — The visual layout that appears in the Vial GUI.
  Shows which key is where on screen. Compiled into the firmware itself.

---

## Matrix Wiring Guide

### How a Key Matrix Works

Instead of wiring every key directly to the MCU (which would need hundreds of pins),
keys are arranged in a grid. Each key sits at the intersection of one ROW wire and one COL wire.
When you press a key, it connects that row and column. The firmware scans each row,
activating it one at a time, and checks which columns are connected — this is how it knows
which key was pressed.

**Diodes** prevent "ghosting" — phantom keypresses that would occur if multiple keys are
pressed simultaneously without them.

### Diode Direction: ROW2COL

**ROW2COL** means current flows from ROW → through the diode → through the switch → to COL.

- The **cathode** (striped end / the end with the band) of each diode connects toward the **ROW wire**.
- The **anode** (plain end) connects toward one pin of the switch.
- The other switch pin connects to the **COL wire**.

In plain terms: the stripe on the diode points AWAY from the switch, toward the row.

### Row Pin Assignments

Each row wire on your PCB/hand-wire connects to this GPIO pin on the MCU:

  - Row 0: **D4**
  - Row 1: **C6**
  - Row 2: **D7**
  - Row 3: **E6**

### Column Pin Assignments

Each column wire connects to this GPIO pin on the MCU:

  - Col 0: **B1**
  - Col 1: **B3**
  - Col 2: **B2**
  - Col 3: **B6**
  - Col 4: **B4**
  - Col 5: **B5**
  - Col 6: **F5**
  - Col 7: **F6**
  - Col 8: **F7**
  - Col 9: **D0**


---

## How to Build the Firmware

### Option A: GitHub Actions (Recommended — no software to install)

1. Create a GitHub repository.
2. Place the contents of this zip into the repo root (so `.github/` and `keyboards/` are at the top level).
3. Push to GitHub.
4. Go to **Actions** → latest workflow run → wait for it to complete.
5. Download the firmware artifact (zip containing .uf2 or .hex).

### Option B: Local Build

1. Clone vial-qmk: `git clone --recurse-submodules https://github.com/vial-kb/vial-qmk.git`
2. Copy `keyboards/handwired/es_sparrow/` into `vial-qmk/keyboards/handwired/`
3. From the vial-qmk root: `make handwired/es_sparrow:vial`
4. The firmware file will appear in the `vial-qmk/` root or `.build/` folder.

---

## How to Flash

### RP2040 (Raspberry Pi Pico)
1. Hold the **BOOTSEL** button on the Pico while plugging in USB.
2. A drive called **RPI-RP2** appears on your computer.
3. Drag the **.uf2** file onto that drive. It flashes and reboots automatically.

### ATmega32U4 (Pro Micro / Elite-C)
1. Open **QMK Toolbox**.
2. Load the **.hex** file.
3. Press the **Reset** button on the PCB (or short RST to GND twice quickly).
4. QMK Toolbox will detect the bootloader and flash automatically.

### STM32 (Proton C / WeAct)
1. Hold the **BOOT** button while plugging in USB (enters DFU mode).
2. Use **QMK Toolbox** or `dfu-util` to flash the **.bin** file.

---

## How to Use Vial

1. Flash your keyboard.
2. Download Vial from [https://get.vial.today](https://get.vial.today) or use the web version.
3. Plug in the keyboard. Vial should auto-detect it.
4. If prompted to **unlock**, hold the unlock combo: **top-left key + bottom-right key** simultaneously.
5. Click any key on the visual layout and press the new key you want to assign.
6. Changes save to the keyboard's EEPROM instantly — no reflashing needed.

---

## Modifying This Firmware

### Change a key's function permanently (in firmware)
Edit `keymaps/vial/keymap.c`, change the keycode, rebuild and reflash.
Full keycode reference: https://docs.qmk.fm/keycodes

### Change which pins are rows/columns
Edit `keyboard.json` → `matrix_pins` → `rows`/`cols`.
Make sure the new pins don't conflict with the TRRS serial pin (if split).

### Add a layer
In `keymap.c`, add a new `[1] = LAYOUT(...)` block with KC_TRNS for keys you want transparent.
Then assign a layer-switch key (e.g. MO(1)) on layer 0 to activate it.

### Change the USB device name
Edit `keyboard.json` → `keyboard_name`.

### Regenerate the Vial UID
If you fork this keyboard for a new design, run from the vial-qmk root:
`python3 util/vial_generate_keyboard_uid.py`
Then paste the result into `keymaps/vial/config.h`.

---

## About This Software

**QMK / Vial Wizard** is a free, browser-based tool for generating complete Vial-QMK
firmware configuration files from scratch — no command line required.

| | |
|--|--|
| **Author** | Robin Gosse |
| **URL** | https://www.robingosse.ca/qmkwizard |
| **Version used** | v5.6 |
| **Date of this revision** | 2026-02-28 |
| **Generated on** | 2026-09-05 |
| **Firmware target** | Vial-QMK (https://github.com/vial-kb/vial-qmk) |
| **Based on** | Official QMK docs (https://docs.qmk.fm) + Official Vial docs (https://get.vial.today/docs) |

### Version History

| Version | Type | Notes |
|---------|------|-------|
| 1.0 | Major | Initial release — form fields, generates basic keyboard.json + rules.mk |
| 2.0 | Major | Added visual layout grid, thumb cluster, split/TRRS support |
| 3.0 | Major | Full rebuild — drag-and-drop layout editor, live split mirror preview, mono/split radio cards, Suggest a Feature FAB |
| 3.1 | Minor | Added handedness pin, README generation, heavy file comments, version tracking |
| 3.2 | Minor | Added per-MCU pin recommendation chips with hover tooltips explaining WHY each pin is suggested; nice!nano ZMK warning; avoid-pin panels |
| 3.3 | Minor | Replaced Suggest-a-Feature FAB with slide-up feedback drawer; name, contact, referral source, comments; sends via mailto |
| 4.0 | Major | Added Features step (step 4): 14 Easy features — layer count, macro buffer, macro count, RGB defaults+sleep, debounce time+algorithm, Key Lock, factory reset key, combo keys, USB device version, USB feature flags, bootmagic key control, Force NKRO, Joystick toggle, QMK branch/commit selection |
| 4.1 | Minor | Added QMK documentation links throughout — every section and feature links to the relevant official docs.qmk.fm page or anchor |
| 5.0 | Major | Added Intermediate tier features: animated RGB indicators (Caps Lock, Num Lock, Win key lock, NKRO status) with per-LED colour picker, blink support, global brightness follow, and lock-logic inversion; diagnostics keycodes that type debounce settings and a configurable test string with optional auto-repeat; code generator refactor (genCustomKeycodes, genProcessRecordUser, genMatrixScanUser, genIndicatorCode); fixed mirror thumb index bug in canvas renderer; removed dead code |
| 5.6 | Bug Fix | Removed features block (nkro, lto) from keyboard.json — vial-qmk's vial branch schema rejects 'lto' and discards the entire file, causing "No LAYOUTs defined", no bootloader, and AVR fallback crash; moved LTO_ENABLE=yes and NKRO_ENABLE=yes to keyboard-level rules.mk instead |
| 5.5 | Minor | Handedness dropdown redesigned: 4 single-side options (Left to GND / Left to VCC / Right to GND / Right to VCC) — describes only the wire you actually run; hint clarifies the other half floats high via internal pull-up; README wiring guide updated to match — no resistor mention, correct single-wire instructions |
| 5.4 | Bug Fix | vial.json: removed // comments from right-half row arrays (invalid JSON, caused Vial build failures); keyboard.json: removed "split_keyboard" from features block (not a valid key — split already configured under "split" section); Workflow: added pre-rsync directory existence check with clear error message explaining the extract-then-push requirement; Step 5: added prominent Vial-QMK repo link panel (github.com/vial-kb/vial-qmk, branch: vial) |
| 5.3 | Minor | Handedness dropdown: descriptive labels (Left to VCC / Left to GND etc); canvas key alignment fixed to SNAP grid (15px padding instead of 20px — keys now on-grid on first load); General/Advanced feature tabs — macro buffer, debounce algorithm, USB flags, indicators, diagnostics, cycling, build options hidden in General; bootmagic replaced with layout key picker (select from actual keys); factory reset gets key picker with optional keymap.c pre-assignment |
| 5.2 | Minor | RP2040 pin inputs: enter numbers only (GP prefix prepended automatically), normalizePin() helper normalizes GP/bare number entries throughout; handedness pin level dropdown: added Low = Left half and Low = Right half options for all 4 wiring combinations; updated config.h and README wiring guide for new options |
| 5.1 | Minor | Cleanup: fixed redundant rows_ variable, removed duplicate syncStep1Recs() init call, moved wizard constants to top of constants section; New: Key Cycling (#69) — cycling keys send different keycodes on successive presses, generates custom enum + tap_code16() switch cases; Slow typeout (#76) — optional character delay switches test string from send_string() to send_string_with_delay(); #41 already handled by canvas active/inactive toggle |

---

*This firmware was generated by the QMK / Vial Wizard. All hardware configuration
is based solely on the official QMK documentation (docs.qmk.fm) and official Vial
documentation (get.vial.today/docs). No third-party guides were used.*
